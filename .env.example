# File: .env.example
# Application
APP_PORT=8080

# Database
DB_HOST=localhost
DB_PORT=5432
DB_USER=admin
DB_PASSWORD=secret
DB_NAME=dealls_db

# JWT
JWT_SECRET=a-very-strong-and-secret-key

# Seeder (set to "true" on first run to populate the database)
RUN_SEEDER=true